#
# https://docs.spring.io/spring-boot/docs/current/reference/html/appendix-application-properties.html
#

# this is an example configuration file in test profile
# this should overrides base configuration with values of local settings

# Database configuration
spring.datasource:
  url: jdbc:mysql://${MYSQL_SERVER:localhost}:3306/${MYSQL_DATABASE:mydatabase}
  username: ${MYSQL_USER:myuser}
  password: ${MYSQL_PASSWORD:secret}
